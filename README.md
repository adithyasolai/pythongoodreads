# pythongoodreads

Using python to search through the dataset to find the books with the highest average rating (in different # of ratings categories), highest # of ratings, etc.

The pandas library was used to store these values in a DataFrame.
